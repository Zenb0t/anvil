name: oracle-validation-example

on:
  workflow_dispatch:

jobs:
  oracle:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout oracle repo
        uses: actions/checkout@v4
      - name: Checkout product repo at SHA
        run: |
          echo "Fetch product repo and checkout exact commit SHA"
      - name: Build product artifacts
        run: |
          echo "Build product target under test"
      - name: Run oracle suite
        run: |
          echo "Execute oracle-tests against artifacts"
